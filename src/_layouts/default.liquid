<html data-theme="georgia">
  <head>
    <title>{{ title }}</title>
    <link href="/css/mcss.css" rel="stylesheet" />
  </head>
  <body>
    <header>
      <h1>{{ title }}</h1>
      <p>{{ description }}</p>
    </header>
    <hr>
    <main>
      <article>
        {{ content }}
      </article>
    </main>
    <hr>
    <footer>
      Written by {{ author }} on {{ date }}.
    </footer>
  </body>
</html>