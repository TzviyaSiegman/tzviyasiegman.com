module.exports = function(eleventyConfig) {
    // Return your Object options:
    return {
      dir: {
        input: "src/",

        // relative to `input` above
        layouts: "_layouts"
      }
    }
  };